# Copy this file to .env (same folder) and set the password.
# Terraform reads ISE_HOST, ISE_USERNAME, ISE_PASSWORD from .env (see README).
# ZTP ISE user is iseadmin. Do not commit .env.

ISE_HOST=192.168.1.90
ISE_HOSTNAME=pan1
ISE_USERNAME=iseadmin
# Put the lab admin password here. Do not commit .env.
ISE_PASSWORD=changeme
ISE_GATEWAY=192.168.1.10
ISE_NTP=192.168.1.10
ISE_DNS=192.168.1.46
ISE_DOMAIN=test.lab
ISE_NETMASK=255.255.255.0
ISE_MASK=/24

# NAD TACACS shared secret. Required for a normal apply (default pushes all 6250 NADs).
# Leave empty ONLY if you set TF_VAR_nad_count=0 (policy-only, no switches).
# Do not commit a real secret. .env is gitignored.
NAD_TACACS_SECRET=
